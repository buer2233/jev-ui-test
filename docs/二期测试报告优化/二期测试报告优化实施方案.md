# 二期测试报告优化实施方案

> 配套文档：[二期测试报告优化可行性分析报告.md](./二期测试报告优化可行性分析报告.md)
>
> 本方案把三条需求 + 六个已确认决策拆成可执行的改造步骤。所有设计决策都标注**依据**
> （实测数据或项目既有约束），并区分「**已验证**」与「**待实测**」。
>
> **本文随实施更新**：每完成一个里程碑，更新 §11 的进度台账与对应里程碑的状态标记。
> 台账里**只记录真跑过的结果**，没跑的一律写「未开始」——不写"应该可以"。

---

## 0. 方案摘要

### 0.1 六个设计决策一览

| # | 决策 | 落地方式 | 依据 |
|---|---|---|---|
| 1 | **步骤边界拆分**：浏览器动作要从「决策」步骤里拿出来 | `runner.py` 的 `_tick_once()` 拆成 predict / act 两段，各自成步骤 | 实测：12 个「执行」步骤共 **8 ms**、12 个「决策」步骤共 **23,006 ms**——动作实际发生在决策步骤内（分析报告 §1.4） |
| 2 | **决策步骤显示传入参数** | 参数表（公开 API）+ 完整请求体附件；**输出侧**才用事后补的私有路径 | 分析报告 §2.3：三条载体路线全部实测过 |
| 3 | **执行步骤显示请求参数与返回值** | 同上；另需库侧接收 `browser.act()` 的返回值并补进 `history` | `agent.py:117` 现在丢弃了返回值 |
| 4 | **录屏三档开关** | `0` 不记录 / `1` 全部（默认）/ `-1` 仅失败；**`-1` 的成功用例不编码、不附加** | 实测：30 秒实录 → **3.5 MB** mp4、编码 **7.15 s**；原始帧 **92 MB/分钟** |
| 5 | **等待独立成步骤（≥200 ms）+ 双开关** | 库侧只记观测值；runner 渲染步骤并**事后改 `start`/`stop`** 拿真实区间 | 实测：事后改步骤时间**生效**（351 ms 真实区间落盘，探针 6） |
| 6 | **二开插件跟框架一起入库 + 版本锁定** | `framework/report_plugin/step-video/` + `scripts/install_report_plugin.py`（幂等 + 版本校验）+ `versions.json` | 实测：`allure.api` 是 CLI 版本相关的扩展点；插件跑通证据见分析报告 §4.4 |

**配置优先级**（决策 1 的答复，沿用仓库既有范式）：

```text
pytest 参数  >  环境变量 JEV_NL_*  >  config.json  >  framework/config.py 内置默认
```

### 0.2 已验证 / 待实测 一览

| 项 | 状态 | 证据 |
|---|---|---|
| 步骤参数能渲染（公开路 + 装饰器路 + 事后补） | ✅ 已验证 | 分析报告 §2.3，探针 1/2/4 |
| 事后改步骤 `start`/`stop` | ✅ 已验证 | 探针 6 |
| Allure 内嵌播放 mp4 | ✅ 已验证 | 分析报告 §4.1 |
| CDP 帧时间戳 = Unix 纪元秒（可与步骤时间对齐） | ✅ 已验证 | 分析报告 §4.2.1 |
| 对齐精度：`concat + cfr 30` → 中位 17 ms / 最坏 33 ms | ✅ 已验证 | 分析报告 §4.2.2 |
| 二开插件端到端跑通（真实报告渲染出区块） | ✅ 已验证 | 探针插件 + `plugin-proof.png` |
| 起点纪元用 `label` 而非 `parameter`（后者改 `historyId`） | ✅ 已验证 | 分析报告 §4.2.3 |
| **录屏的端到端 A/B**（开/关录屏跑完一条用例） | ❌ **待实测** | §13 第 1 项 |
| **等待开关的 A/B**（比决策请求数） | ❌ **待实测** | §13 第 2 项 |
| 「等待」步骤在 E9 真实用例上的实际收益 | ❌ **待实测** | §13 第 3 项 |

### 0.3 实施进度总览

> 详见 §11 的台账。**M1 是其余一切的前提，必须先做。**

```text
M1 步骤边界拆分        ⬜ 未开始   ← 阻塞 M2/M3/M5
M2 参数表与附件        ⬜ 未开始   ← 依赖 M1
M3 配置开关            ⬜ 未开始
M4 等待步骤            ⬜ 未开始   ← 依赖 M1 + M3
M5 录屏与合成          ⬜ 未开始   ← 依赖 M3
M6 二开插件与版本校验  ⬜ 未开始   ← 依赖 M1（步骤要有真实区间）
```

---

## 1. 改造范围总览

### 1.1 新增文件

| 文件 | 职责 |
|---|---|
| `jev_ultrafast/framework/report_params.py` | 步骤参数的两个把手：进入前设 `.params`（公开）、事后补（私有 API 收口在这里） |
| `jev_ultrafast/framework/video.py` | 录屏：启动 screencast、采集线程、epoch 锚点、停止、三档清理决策 |
| `jev_ultrafast/framework/encode.py` | 合成：jpeg 帧 → mp4（`concat` + `-fps_mode cfr -r 30`） |
| `jev_ultrafast/framework/report_plugin/step-video/index.js` | Allure 插件（浏览器侧）：步骤时间轴 + 点步骤 seek 视频 |
| `jev_ultrafast/framework/report_plugin/step-video/styles.css` | 插件样式 |
| `jev_ultrafast/framework/report_plugin/step-video/versions.json` | **版本锁定表**（机器可读，给后处理脚本校验用） |
| `jev_ultrafast/framework/report_plugin/step-video/README.md` | 这个插件依赖哪一版 Allure、升级时要回归什么 |
| `scripts/install_report_plugin.py` | 后处理：校验版本 → 装插件 → 改 `index.html`（幂等） |
| `tests/test_report_params.py` | 参数把手的单测（含"进入后改无效"的反向断言） |
| `tests/test_video_modes.py` | 三档清理决策的单测（纯函数，不碰浏览器） |
| `tests/test_report_plugin.py` | 版本校验 + 幂等安装的单测 |

### 1.2 修改文件

| 文件 | 改动 | 规模 |
|---|---|---|
| `jev_ultrafast/framework/runner.py` | **步骤边界拆分**（核心改动）；参数表与附件；等待步骤；录屏接线 | 大 |
| `jev_ultrafast/framework/reporting.py` | `attach_decision` 补字段；新增 `attach_execution()`、`attach_step_params()` | 中 |
| `jev_ultrafast/framework/config.py` | 新增三个配置项的默认值 | 小 |
| `jev_ultrafast/framework/e9_config.py` | 读 `config.json` 的新键 | 小 |
| `tests/conftest.py` | `pytest_addoption` 加三个选项 | 小 |
| `jev_ultrafast/browser.py` | **库侧**：记录等待观测值；`wait_stable` 开关 | 小 |
| `jev_ultrafast/agent.py` | **库侧**：接收 `browser.act()` 返回值写进 history；history 补元素标识；透传 `wait_stable` | 小 |
| `AGENTS.md` | 新增契约：录屏三档、等待开关、二开版本锁定 | 小 |
| `README.md` / `config.example.json` | 同步新增配置项与命令 | 小 |
| `pyproject.toml` | 无需改（`allure-pytest==2.13.5` 已钉） | — |

### 1.3 边界：**不动**的东西

| 不动 | 理由 |
|---|---|
| `snapshot.js` | 本期不涉及元素发现 |
| 新鲜度校验 / 遮挡校验 / `elementFromPoint` | 分析报告 §5.6：**只读能力可以扩，安全校验一条都不放松** |
| 变更操作的重试策略 | 录屏与参数显示都是只读观察，不碰"动作绝不重试" |
| `allure-pytest==2.13.5` 的锁 | ≥2.14 会让 2.13.8 的 CLI **静默丢全部结果** |
| Allure CLI 本体 | 二开走后处理 + 插件 API，不改 CLI 安装目录 |

---

## 2. 改造后的分层与数据流

```text
runner.py（框架层：步骤包装、参数、附件、开关解析）
  │
  ├─ resolve_reports(agent, case, config)          ← 解析四级优先级，得到三个开关的值
  │
  ├─ video.Recorder(agent.browser, mode)           ← 录屏（只读：Page.startScreencast）
  │     └─ 采集线程 drain_events() → artifacts/<case>/frames/NNNNNN.jpg
  │
  ├─ 每轮循环：
  │     ├─ [步骤] 决策        ← predict；参数表 + 请求体附件
  │     ├─ [步骤] 等待         ← 读库记的 last_wait，≥阈值才建（事后改 start/stop）
  │     └─ [步骤] 执行        ← act；参数表 + 请求/返回附件
  │
  ├─ 终局断言 + 终局判定
  │
  └─ video.finish(verdict)                        ← 三档决策：编码/不编码、附加/不附加
        └─ encode.to_mp4(frames, epoch) → allure.attach(..., MP4)
              + allure.dynamic.label("videoEpochMs", epoch)
```

**库侧只增加"可观测性"，不增加"Allure 依赖"**：

| 库侧新增 | 形态 | 为什么不算越界 |
|---|---|---|
| `browser.last_wait` | 一个 dict，记"等了多久、等到没、轮询了几次" | 与 `history` 同类——把发生的事记下来，供上层渲染 |
| `agent.py` history 补 `node`/`role`/`execute_result` | 几个字段 | history 本来就该自包含；与 Allure 无关 |
| `wait_stable` 开关 | 关键字参数，默认 `True` | 默认值保持现有行为不变 |

> **不可越界的那条线**：`agent.py` / `browser.py` **永远不 import allure**。
> 步骤包装、参数、附件全部留在 `framework/`。这条在 AGENTS.md 里已有明文，本方案继续遵守。

---

## 3. 关键设计一：步骤边界拆分（M1，其余一切的前提）

### 3.1 现状与目标

**现状**（`runner.py:23-42` 的 `_tick_once` 与 `runner.py:98-119`）：

```python
def _tick_once(agent):
    agent.command("predict")                     # ← 决策
    if agent.state["status"] in {"done", "blocked"}:
        return None
    try:
        return agent.command("act", {...})        # ← 执行（同一个步骤体内！）
    except StalePage:
        ...
```

```python
with allure.step(f"第 {steps} 步 · 决策（Jev 概率判断）"):
    _tick_once(agent)                            # predict + act + 页面稳定等待 都在这里
```

**目标**：predict 与 act 各自成为独立步骤，且「执行」步骤**真的包住执行这件事**。

### 3.2 拆分后的循环骨架

```python
while agent.state["status"] not in {"done", "blocked"}:
    ...预算检查...

    steps += 1
    before_actions = len(agent.state["history"])

    # ── 决策步骤 ────────────────────────────────────────────────
    ctx = allure.step(f"第 {steps} 步 · 决策（Jev 概率判断）")
    ctx.params = {"步序": steps, "页面": agent.state["page"]["url"],
                  "候选元素数": len(agent.state["page"]["actions"]),
                  "模型": os.environ.get("TYPESAFE_MODEL", "jev-latest")}     # 公开路 1
    with ctx:
        agent.command("predict")
        decision = agent.state.get("decision") or (
            agent.state["decisions"][-1] if agent.state["decisions"] else None)
        if decision:
            report_params.fill_decision_outputs(ctx.uuid, decision)            # 事后补（私有）
            attach_decision(decision)

    if agent.state["status"] in {"done", "blocked"}:
        break

    # ── 决策落定、执行之前：抓现场 ──────────────────────────────
    page_before = agent.state["page"]
    selected = decision["choice"]
    # 这个查找不可能失败：selected 是 validate_choice 对着本页动作空间校验过的，
    # 而 page_before 就是那次决策所依据的同一份观测。真找不到时 act 里也会同样地
    # next(...) 抛 StopIteration——保持与库一致，这里不额外兜。
    action = next(a for a in page_before["actions"] if a["id"] == selected)

    # ── 执行步骤 ────────────────────────────────────────────────
    ctx = allure.step(f"第 {steps} 步 · 执行 {decision['operation']} → {action['label']}")
    ctx.params = {
        "操作": decision["operation"], "目标索引": decision["target"],
        "元素节点": action["node"], "角色": action["role"],
        "执行前值": action.get("value", ""),
        "是否生成文本": "是" if action["kind"] == "fill" else "否",
    }
    with ctx:
        try:
            agent.command("act", {"fingerprint": page_before["fingerprint"]})
        except StalePage:
            report_params.mark(ctx.uuid, "页面已过期，本次执行被丢弃")           # 留痕
            _reset_after_stale(agent)
            continue

    # 执行后的动作记录（现在 history[-1] 一定有）
    if len(agent.state["history"]) <= before_actions:
        continue
    attach_execution(agent, action, agent.state["history"][-1])

    # ── 等待步骤：紧跟在产生它的执行步骤【之后】，见 §7.2 ───────
    _emit_wait_steps(agent, steps, threshold_ms)
```

### 3.3 四个必须保住的语义（改错一条报告就是错的）

| # | 语义 | 现状位置 | 拆分后怎么保 |
|---|---|---|---|
| 1 | **`DONE`/`BLOCKED` 不产生动作，不建执行步骤** | `runner.py:106-114` | `break` 前不建执行步骤；执行步骤只在 `history` 真的增长后建 |
| 2 | **不把上一步的动作重复显示成本步结果** | `runner.py:110-114` 的注释 | 用 `before_actions` 快照比较，与现在同法 |
| 3 | **`StalePage` 的语义**：丢弃决策 → 重观察 → 状态回 `ready` | `runner.py:34-42` | 抽成 `_reset_after_stale(agent)`，predict 与 act 两侧共用；**两处都要保留** |
| 4 | **步数编号成对** | `runner.py:98/115` 共用 `steps` | 一轮内 `steps` 只自增一次；决策/等待/执行三个步骤共用同一个 `steps` |

**外加一条新增要求（分析报告 §1.4 第 4 点）**：**"决策被丢弃"必须留痕**。
实测 12 次决策只产生 10 条动作，其中第 1 步的决策因页面过期被丢弃，而报告里它与 `DONE` 长得一样。
拆分后要在被丢弃的那一轮**显式记一个附件或参数**，否则"跑了 12 次决策"会被误读成"点了 12 次"。

### 3.4 验收判据（M1 的 Definition of Done）

1. **同一用例拆分前后对比**：逐步骤核对标题、时长、附件数量。
2. **执行步骤的时长不再是 0**（或至少不再是恒为 0–2 ms），且决策步骤的时长**明显下降**。
   用附录 A 第 6 条的命令逐步骤打印时长即可。
3. **步骤数**：一轮循环从 2 个变 2 个（等待未达阈值时）或 3 个（等待达阈值时）。
   `DONE` 那一轮**少一个执行步骤**，与拆分前一致。
4. **丢弃留痕**：能看出"这一轮决策被丢弃了"。
5. `uv run ruff check .` 全绿；`uv run pytest` 离线全绿。

---

## 4. 关键设计二：步骤参数与附件（M2）

### 4.1 三条参数路线的用法边界

（全在分析报告 §2.3 实测过，这里只定"哪里用哪条"）

| 路线 | 用途 | 限制 |
|---|---|---|
| **路 1**：`allure.step()` 返回的 StepContext，**进入前**设 `.params` | **所有进入前就知道的量**（步序、URL、元素节点、操作、目标索引） | 必须在 `with` **之前**设；进入后再改**无效** |
| **路 4**：`reporter.get_item(uuid)` 事后补 | **只有返回后才知道的量**（选中概率、决策耗时、**重发次数**、浏览器返回值） | 私有 API，**只在这一处用**，全部收口到 `report_params.py` |
| 装饰器 | **不用** | 值会被 `repr()` 加引号（`'CLICK'`），且拿不到输出侧 |

### 4.2 `framework/report_params.py` —— 把私有路径收口成一个文件

```python
"""步骤参数的两个把手。

为什么单独一个文件：路 4 用了非公开契约（插件名 + reporter.get_item），
升级 allure-pytest 时只需要重验这一个文件，而不是满仓库找。

实测依据（分析报告 §2.3）：
  · 进入 with 之后再改 ctx.params 是【无效】的——params 只在 start_step 那一刻被读走；
  · 事后补 step.parameters / step.start / step.stop 是【有效】的。
"""

def step(title, params=None):
    """公开路 1：返回 StepContext，params 在进入前设好。"""
    ctx = allure.step(title)
    ctx.params = dict(params or {})
    return ctx


def _reporter():
    """从 pytest 插件管理器取 allure 的 reporter。

    插件名 "allure_listener" 是实测查出来的（不是猜的）；
    取不到就【响亮报错】——静默失败会让所有参数悄悄消失。
    """
    ...


def fill_outputs(uuid, **params):
    """路 4：事后把输出侧的量补进参数表。"""
    ...


def set_times(uuid, start_ms, stop_ms):
    """路 4：把步骤的起止时间改成真实区间（给"等待"步骤用）。"""
    ...


def mark(uuid, note):
    """给步骤补一条文本参数，用于留痕（如"页面已过期，本次执行被丢弃"）。"""
    ...
```

**必须配的单测**（`tests/test_report_params.py`）：

- `test_params_set_before_enter_are_recorded`
- `test_params_set_after_enter_are_ignored` —— **反向断言**，守住那个坑
- `test_outputs_are_appended`
- `test_times_override_the_automatic_ones`
- `test_missing_reporter_fails_loudly` —— 取不到 reporter 必须报错，不能静默

### 4.3 决策步骤：补哪些字段

`reporting.attach_decision` 现在只挂 7 个字段，`model.choose()` 返回 **14 个**。本次补齐：

| 去处 | 字段 |
|---|---|
| **参数表**（路 1 + 路 4） | 步序、页面 URL、候选元素数、模型名（路 1）<br>选中操作、目标索引、操作概率、目标概率、置信度、决策耗时ms、**决策重发次数**（路 4） |
| **附件**（完整载荷） | `request`（完整请求体）、`raw_answers`（原始响应）、`operation_probabilities` / `target_probabilities`（全量分布） |

**体积控制**（实测：单次请求体 9–20 KB）：
- 完整请求体挂附件，**不挂 `state.page.text`**（可省一半以上）；
- 全量概率分布进附件，**不进参数表**（E9 场景下候选可能有 10+ 个，参数表会很长）。

**`decision_attempts > 1` 要显眼**：它是"响应不合法、重发过"的信号，
AGENTS.md 明确要求"重发要看得见，不静默自愈"。参数表里直接给数字。

### 4.4 执行步骤：请求参数与返回值

| 去处 | 内容 |
|---|---|
| **参数表**（路 1） | 操作、目标索引、元素节点、角色、执行前值、是否要生成文本 |
| **附件**（请求/返回） | `请求`：完整 action 对象 + 写入文本<br>`生成文本`：值 / 模型 / 耗时ms<br>`返回`：`browser.act()` 的返回值<br>`执行后`：`page_changed` / `url` / `elapsed_ms` |

**注意一个拿不到的东西**：`TYPE_TEXT` 要写的文本是在 `agent.command("act")` **内部**
由文本模型生成的（`agent.py:106-115`），执行者在进入步骤前**不知道要写什么**。
所以"写入文本 / 文本模型耗时"**进不了参数表**，只能进附件。
**不要为了让它进参数表去把文本生成从库里挪出来**——那会改变 `pending_text` 复用与新鲜度校验的既有语义。

### 4.5 库侧改动（两处小改，都在"history 更完整"的范畴内）

```python
# agent.py —— 现状：结果被丢掉
state["browser"].act(action, page, text=text)

# 改成：接收下来，写进 history
execute_result = state["browser"].act(action, page, text=text)
```

```python
# agent.py:121-141 的 history 构造 —— 补三个字段，让 history 自包含
state["history"].append({
    ...现有字段...,
    "node": action["node"],           # 元素标识：不补就只看到"进入官网"，看不到点的是哪个节点
    "role": action["role"],
    "value_before": action.get("value", ""),
    "execute_result": execute_result,  # {"executed": "<action id>"}
})
```

**为什么这是库侧合理改动**：`history` 是 `snapshot()` 的一部分，会被整个挂进报告的「执行轨迹」附件。
它缺元素标识，任何上层（报告、inspector、调试脚本）都补不回来——因为执行后 `state["page"]` 已被替换。

### 4.6 落盘校验（怎么确认参数真的进了报告）

**不能只看"代码写了"**。每个里程碑都要跑一次落盘校验：

```bash
uv run pytest artifacts/... --alluredir=<dir> -q
uv run python -c "
import json, glob, sys; sys.stdout.reconfigure(encoding='utf-8')
d = json.load(open(sorted(glob.glob('<dir>/*-result.json'))[0], encoding='utf-8'))
for s in d['steps']:
    print(s['name'], '| params:', json.dumps(s.get('parameters'), ensure_ascii=False))
    print('   附件:', [a['name'] for a in s.get('attachments', [])])
"
```

期望：决策步骤有参数表 + 2 个附件；执行步骤有参数表 + 1 个附件；等待步骤有真实区间。

---

## 5. 关键设计三：配置开关（M3）

### 5.1 三个配置项

| 配置项 | 取值 | 默认 | 环境变量 | pytest 参数 |
|---|---|---|---|---|
| 录屏档位 | `0` / `1` / `-1` | **`1`** | `JEV_NL_VIDEO_RECORD` | `--video-record=0\|1\|-1` |
| 页面稳定等待 | `true` / `false` | **`true`** | `JEV_NL_WAIT_STABLE` | `--wait-stable` / `--no-wait-stable` |
| 等待成步骤的门槛 | 毫秒 | **`200`** | `JEV_NL_WAIT_STEP_MS` | `--wait-step-ms=200` |

`config.json` 里新增同名的三个键（`video_record` / `wait_stable` / `wait_step_threshold_ms`），
并把它们写进 `config.example.json`（入库的是占位模板）。

### 5.2 ⚠️ 为什么不能放进 `framework/config.py` 的模块级常量

`config.py` 现在是这样读环境变量的：

```python
DEFAULT_AI_THRESHOLD = float(os.environ.get("JEV_NL_THRESHOLD", "0.75"))
```

**这是 import 期求值**，而 **pytest 参数要到 `pytest_configure` 之后才可用**。
所以"pytest 传值 > 环境变量"这一条**不可能**在 `config.py` 的模块级常量里实现。

**做法**：`config.py` 只放**内置默认值**（不含环境变量），由 runner 在**每个用例开始时**解析一次：

```python
# framework/config.py —— 只放兜底默认
DEFAULT_VIDEO_RECORD = 1        # 0 不记录 / 1 全部 / -1 仅失败
DEFAULT_WAIT_STABLE = True
DEFAULT_WAIT_STEP_MS = 200
```

```python
# framework/runner.py —— 四级优先级在这里解析，逐用例一次
def resolve_report_options(case, pytest_config):
    """优先级：pytest 参数 > 环境变量 > config.json > 内置默认。

    形态照抄 assertions.resolve_threshold()（那个已经是"默认值 + 逐用例覆盖"的现成实现）。
    """
```

> `config.py` 原有的 `DEFAULT_AI_THRESHOLD` 等**保持不动**——那三个是已经上线并被
> YAML 逐用例覆盖着的，贸然重构会动到语义。**只新增，不改旧。**

### 5.3 `conftest.py` 的选项

```python
parser.addoption("--video-record", default=None, choices=["0", "1", "-1"], metavar="MODE",
                 help="录屏档位：0 不记录 / 1 记录全部 / -1 仅保留失败用例（默认取配置）")
parser.addoption("--wait-stable", dest="wait_stable", action="store_true", default=None,
                 help="启用页面稳定等待（默认取配置）")
parser.addoption("--no-wait-stable", dest="wait_stable", action="store_false",
                 help="关闭页面稳定等待。⚠️ 可能推高付费决策请求数，见实施方案 §7.4")
parser.addoption("--wait-step-ms", default=None, metavar="MS",
                 help="等待耗时超过该值才独立成报告步骤（默认取配置）")
```

**`default=None` 是关键**：`None` 表示"没显式传"，据此才能实现"未传用配置默认、传了用传值"。
`store_true`/`store_false` 共用 `dest="wait_stable"`，两个都不传时是 `None`。

### 5.4 本期不加用例级（YAML）覆盖

理由：你确认的优先级是四级，且没有提出"逐用例调录屏"的需求。
`resolve_*` 的形态天然支持后续加 `case.get("video_record")`，
**但本期不加**——避免一边做报告优化一边扩张用例 schema。
唯一的例外是**负向对照用例**：它们设计上就该失败，`--video-record=-1` 正合其用，
不需要 YAML 字段。

---

## 6. 关键设计四：录屏与合成（M5）

### 6.1 录制放在框架层，不动库

`browser.py` 已有一个 `record_dir` 机制（`agent.py:149-152`），但它**每个动作只存一张截图**，
不是连续录屏，不足以合成视频。所以本期的录制**新写一个框架侧组件**，只读地用 CDP：

```python
# framework/video.py
class Recorder:
    """后台标签页的连续录屏。

    只读：Page.startScreencast 是协议级截屏，不碰页面、不碰用户可见标签页。
    与 scripts/record_flights.py 同一套机制（那份已在线跑过），
    差别是把 epoch 锚点与步骤时间戳严格对上。
    """

    def __init__(self, browser, out_dir):
        self.epoch = time.time()          # ← CDP metadata.timestamp 是 Unix 纪元秒，同基准
        browser.call("Page.startScreencast", format="jpeg", quality=80,
                     maxWidth=1120, maxHeight=780, everyNthFrame=2)
        ...采集线程 drain_events()...
```

**实测参数**（分析报告 §4.1）：`everyNthFrame=2` / q80 / 1120×780 → 约 **20 fps**、帧约 **70 KB**。

### 6.2 合成：必须用 `concat + cfr`，不能用固定帧率

```python
# framework/encode.py
def to_mp4(frames, gaps_ms, out_path, fps=30):
    """jpeg 帧 → mp4。

    为什么不用 `-framerate <平均帧率>`：帧间隔是不均匀的（实测中位 33.8 ms、最大 397 ms），
    固定帧率会把误差弄到 中位 85 ms / 最坏 451 ms，"点第 6 步"可能跳到别的画面。
    concat 把每帧真实 duration 喂进去 + `-fps_mode cfr -r 30` 重采样到均匀网格，
    实测降到 中位 17 ms / 最坏 33 ms。见分析报告 §4.2.2。
    """
```

固定参数（**这三个都不能省**）：

| 参数 | 为什么 |
|---|---|
| `-fps_mode cfr -r 30` | 按真实时间戳重采样（对齐精度 ±1 帧） |
| `-pix_fmt yuv420p` | 否则浏览器可能报 "does not support video tag"（Allure issue #1676） |
| `-movflags +faststart` | moov 前置，边下边播 |

### 6.3 三档与清理时序

| 档位 | 启动录制 | 编码 | 附加 | 原始帧 |
|---|---|---|---|---|
| `0` | **否** | — | — | — |
| `1` | 是 | 是 | 是 | 跑完清 |
| `-1` + 通过 | 是 | **否** | **否** | 跑完清 |
| `-1` + 未通过 | 是 | 是 | 是 | 跑完清 |

**两个要点**：

1. **`0` 必须"不启动"，而不是"录了再删"**——否则白付录屏开销（观察阶段 +3–6 ms/次、采集线程持续轮询）。
2. **`-1` + 通过 要"不编码、不附加"，而不是"生成再删"**——Allure 在 `attach()` 时会把字节
   **拷贝**进 `allure-results/`，附加过之后再删原文件也删不掉报告里那份。
   等价的结果，还省掉 **7 秒**编码。

**清理必须放 `finally`**：帧约 **92 MB/分钟**，30 秒用例先落 ~46 MB。
无论成败、无论异常，帧目录都要清（失败的用例留 mp4，不留帧）。

### 6.4 "通过"的判定边界（**这里最容易出事**）

`-1` 会**删证据**，删了追不回。所以"要留"的边界必须画严：

```python
def should_keep(verdict, note, history, snapshot):
    """-1 档下是否保留录屏。

    只有【明确通过】才不保留。下面这些"通过了但不健康"的情况一律保留：
      · 终局判定 ok=False（自然要留）
      · note 非空（达到步数上限 / 超时提前停止）
      · 过程里出现过 StalePage 重决策、或 decision_attempts > 1
      · pytest 的 RERUN（第一次是失败的）
    """
```

理由：这些运行**恰恰最值得看**——通过了但过程不健康，正是下一轮要查的对象。
并且**删除动作必须写进报告**（一条附件或参数："本次按 `-1` 档未保留录屏"），不能静默。

### 6.5 附加到报告

```python
allure.attach(mp4_bytes, "执行录屏", allure.attachment_type.MP4)
allure.dynamic.label("videoEpochMs", str(int(epoch * 1000)))   # ← 必须是 label，不能是 parameter
```

**`label` 不是 `parameter`**（实测，分析报告 §4.2.3）：`parameter` 会改 `historyId`，
于是**每次运行都变成一条新用例**，History/Trend/Retries 全废——**而报告页面看不出任何异常**。

---

## 7. 关键设计五：等待步骤（M4）

> ### ⚠️ 本节是对可行性报告 §8.5 的一处**归属修正**（2026-09-24 复核时发现）
>
> 可行性报告里写的形态是「在第 N 步的**决策与执行之间**插一个『等待页面稳定』步骤」。
> 复核代码位置后发现**那样会把等待归属到错误的轮次上**：
>
> `browser.py` 里的等待，除了首屏那一次，**其余全部发生在 `act` 内部**
> （`follow_new_tab` 的新标签页稳定等待、`observe()` 的输入后同步等待）。
> 若把等待步骤放在执行**之前**，它显示的是**上一轮 act** 里发生的等待——**错位一轮**，
> 而且报告上完全看不出来（时间区间是真的，只是归属错了）。
>
> **修正后的设计**：等待步骤紧跟在**产生它的执行步骤之后**，用同一个 `steps` 编号，
> 标题写明是哪一类等待。归属正确，且仍然是"独立成步骤"。

### 7.1 库侧：记录**一组**观测值，不 import allure

一次 `act` 里可能发生**不止一次**等待（先 `follow_new_tab` 的稳定等待，再 `observe` 的输入后同步等待），
所以记录必须是**列表、每次 act 前清空**——否则后一次会覆盖前一次，报告里少一个等待。

```python
# browser.py —— 与 Allure 完全无关，只是把"等了多久、等到没"记下来
self.waits = []                    # 本轮动作里发生的所有等待

def _record_wait(self, kind, started, elapsed_ms, **extra):
    self.waits.append({
        "kind": kind,                              # stable / settle / input_sync
        "started_ms": int(started * 1000),         # Unix 纪元毫秒，与 Allure 步骤时间同基准
        "elapsed_ms": elapsed_ms,
        **extra,                                   # stable: 是否等到稳定 / polls / timeout
    })
```

三个记录点，对应 §8.1 里的三处（`kind == "wait"` 的动作不记——它本身就是一个执行步骤）：

| 记录点 | `kind` | 触发场景 |
|---|---|---|
| `Browser.__init__` 的 `wait_until_stable` | `"stable"` | 首屏（E9 上 2–3 s，最大的一笔） |
| `follow_new_tab` 的 `wait_until_stable` | `"stable"` | 点击打开了新标签页（E9 流程表单就是） |
| `observe()` 的 `after_input` 块 | `"input_sync"` | 输入后等控件同步（50 / 200 / 500 ms 三档） |

同时把 `wait_stable` 开关透传进来：

```python
# browser.py
def __init__(self, url, cookies=None, *, wait_stable=True):
    ...
    if wait_stable:
        self.wait_until_stable(timeout=25, interval=0.6)
```

```python
# agent.py
def __init__(self, url, goals, *, cookies=None, record_dir=None, screenshots=False, wait_stable=True):
    self.browser = Browser(url, cookies=cookies, wait_stable=wait_stable)
```

> **这是库公开 API 的扩张**。默认值让现有行为不变，但它是库表面的一部分，
> 评审时要明确——避免下一步有人顺手把 `allure.step` 也加进库里。
>
> **按需记录的代价**：`wait_stable=False` 时**不记**（`waits` 留空），
> 这样报告里不会出现一堆 0 ms 的等待步骤。

### 7.2 runner 侧：≥200 ms 独立成步骤，短等待并进执行步骤的参数

**位置**：紧跟在执行步骤之后，共用同一个 `steps` 编号。

```python
def _emit_wait_steps(agent, steps, threshold_ms):
    """把本轮动作里发生的等待落到报告上。

    门槛 200 ms（已确认的决策）：达到门槛的独立成步骤，短的并进执行步骤的参数。
    理由——每轮都建会塞满 50 ms 级的输入同步等待，报告变流水账、反而看不清重点。
    """
    waits = list(getattr(agent.browser, "waits", ()))
    agent.browser.waits.clear()                     # 消费掉，避免下一轮重复显示

    for wait in waits:
        if wait["elapsed_ms"] < threshold_ms:
            continue
        label = {"stable": "页面稳定", "settle": "文档就绪", "input_sync": "输入后控件同步"}[wait["kind"]]
        ctx = report_params.step(f"第 {steps} 步 · 等待（{label}）", {
            "等待耗时ms": wait["elapsed_ms"],
            "是否等到稳定": wait.get("stable", "不适用"),
            "轮询次数": wait.get("polls", ""),
            "超时上限ms": wait.get("timeout", ""),
        })
        with ctx:
            # 等待发生在库里，runner 包不进 with 块 —— 用事后改时间拿真实区间（§7.3）
            report_params.set_times(ctx.uuid, wait["started_ms"], wait["started_ms"] + wait["elapsed_ms"])

    # 短等待不建步骤，但要并进执行步骤的参数里，否则那点耗时就成了"看不见的成本"
    report_params.fill_outputs(
        <执行步骤的 uuid>, 输入后同步等待ms=..., 标签页稳定等待ms=...
    )
```

**首屏那一次的位置**：它在 `Browser.__init__` 里，循环还没开始。
所以由 runner 在「观察初始页面」**之前**单独取一次，渲染成**前置步骤**
（标题 `前置：等待首屏稳定`），而不是"第 1 步"。归属清晰，也不会污染步数编号。

### 7.3 为什么必须事后改 `start`/`stop`

等待发生在库里，runner **包不进 `with` 块**——所以步骤时间戳会是"进入/退出"的瞬时值，
又会变成一个 0 ms 的空壳（**正是「执行」步骤那个缺陷的同一成因**）。

**这个前提已实测**（探针 6）：事后改 `step.start`/`step.stop` **生效**
（落盘 351 ms 真实区间，对照组 201 ms）。用的是与 §4.2 同一个把手，收口在 `report_params.py`。

### 7.4 为什么不给库加"等待回调"让 runner 真正包住它

有个更"正统"的做法：`Browser(..., on_wait=callable)`，库在每次等待前后调这个回调，
runner 传一个"开 step / 关 step"的函数进去。这样步骤就是真包住的，不需要事后改时间。

**不采用的理由**：
- 那是把一个**框架形状的钩子**塞进库本体——比"多记一个字段"侵入得多；
- `__init__` 里的首屏等待**没法**用回调包（Agent 构造完它已经过了），仍然要走事后改时间的路；
- 于是两套机制并存，反而更复杂。

**代价**：报告的步骤时间戳依赖 `report_params.set_times()` 这一条私有路径。
它已经被 M2 的单测守住，且只在"等待"这一个场景用。

### 7.5 ⚠️ 这个开关省不了钱，可能反而更贵

**必须在开关的 help 文本和文档里写清楚**：

- `wait_until_stable` 存在的全部理由，是防"渲染中途决策 → 立刻 `StalePage` → 重观察 → 重决策"；
- 而每轮重决策都是**一次付费的 TypeSafe 请求**；
- 它节省的只有那几次**免费**的 `observe()`；
- **所以关掉它 → 决策请求数可能上升 → 批量执行更贵。**

**这个开关的定位是"调试/诊断"，不是"回归省钱"。**
如果目标是省钱，**优先调小 `timeout`（25 s → 8 s）**——超时后一样往下走，
但保留了"稳定就立刻返回"的快路径。

**在 §13 的 A/B 数据出来之前，保持默认开启。**

---

## 8. 关键设计六：二开插件与版本锁定（M6）

### 8.1 目录（跟框架一起，纳入 Git）

```text
jev_ultrafast/framework/report_plugin/step-video/
  ├── index.js          # Allure 插件：addTestResultBlock + 时间轴
  ├── styles.css
  ├── versions.json     # 版本锁定表（机器可读）
  └── README.md         # 依赖哪一版 Allure、升级时的回归清单
scripts/install_report_plugin.py
```

形态参照 Allure 官方最小插件 `custom-logo-plugin`（实测其结构为
`allure-plugin.yml` + `<id>-<version>.jar` + `static/**`）。
我们走后处理路线**不需要 jar**，但目录保持同构，便于将来升级到正式的 Java 插件。

### 8.2 插件实现要点（机制全部实测过）

| 要点 | 依据 |
|---|---|
| 用 `allure.api.addTestResultBlock(View, {position:'before'})` 插区块 | 官方 `screen-diff` 插件就是这么用的（源码已确认） |
| `View` 继承 **`Backbone.Marionette.View`**（挂在 `window.Backbone` 下，**没有**独立的 `window.Marionette`） | 实测页面取值：Backbone 1.3.3 / Marionette 3.3.1 |
| 数据从 `this.model.toJSON().testStage` 取 | 实测：`testStage.steps[].time.start/stop` 就在里面（29/29 步骤都拿到了） |
| 附件路径拼法 `'data/attachments/' + attachment.source` | 抄 `screen-diff` |
| seek 公式 `Math.max(0, (step.start − epochMs)/1000)` | 抄 Playwright Trace Viewer（其源码 `videoFrame.tsx`） |
| **把视频 fetch 成 Blob URL 再喂 `<video>`** | 抄 Playwright：注释原文 "so that the player can seek without range requests" —— 绕开服务器 Range 限制 |
| **不用 `#t=` 媒体片段** | 实测：**静默失效**（大文件 + 服务器不支持 Range 时 `currentTime` 停在 0） |
| 自检：`|视频时长 − (末步骤.stop − epochMs)| > 1s` 时**页面上显式报错** | 分析报告 §5.3：错位跳转比没有同步更糟 |

### 8.3 后处理脚本（幂等 + 版本校验）

```python
# scripts/install_report_plugin.py
def install(report_dir):
    """把插件装进已生成的报告。

    allure generate 是 Java CLI，Python 侧不能注册插件，所以只能生成后补一刀。
    幂等：报告会反复重新生成，重复执行不能重复插 <script>。
    版本校验：不匹配就【拒绝安装并报错】，绝不能"装上去看着没事"。
    """
    1. verify_versions()          # 对着 versions.json 校验 CLI / Java / ffmpeg
    2. copytree(plugin_src, report_dir/"plugins"/"step-video")
    3. 往 index.html 的 </body> 前插 <script src="plugins/step-video/index.js"></script>
    4. assert 那行真的在 index.html 里     # ← 别让"插件没装上"变成静默失败
```

### 8.4 版本锁定表

**为什么必须锁**：二开依赖的是**报告运行时的内部结构**，不是稳定协议。
升级任一项，插件都可能**静默失效**——报告照常打开，只是没有时间轴。

| 组件 | 锁定版本 | 二开依赖它的什么 | 失配后果 | 谁钉的 |
|---|---|---|---|---|
| **Allure CLI** | **2.13.8** | `window.allure.api` 的 8 个方法、`index.html` 的 `<script>` 装载点、`plugins/<id>/` 约定、`data/test-cases/*.json` 里的 `testStage.steps[].time` | 插件不加载或渲染不出来 | **本机环境，需校验** |
| **allure-pytest** | **2.13.5** | 步骤结果 JSON 的字段 | ≥2.14 写 `titlePath`，2.13.8 **静默丢全部结果** | `pyproject.toml` 已钉 |
| **allure-python-commons** | **2.13.5** | `TestStepResult` 模型；事后改步骤对象直接操作它 | 参数/时间补记失败 | `uv.lock` 已解析 |
| **Java** | **1.8.0_201** | 2.13.8 CLI 的运行环境 | CLI 起不来 | **本机环境，需校验** |
| **报告内置 Backbone** | **1.3.3** | 插件 View 必须继承**页面上那一份** | 自己打包一份会版本冲突，**可能整个报告白屏** | **CLI 自带，需校验** |
| **报告内置 Marionette** | **3.3.1** | `Backbone.Marionette.View.extend(...)` | 同上 | **CLI 自带，需校验** |
| **ffmpeg** | **8.1.1**（需 libx264） | `-fps_mode cfr` / `-movflags +faststart` / `-pix_fmt yuv420p` | 参数不识别或编码放不出来 | **本机环境，需校验** |
| **pytest** | **8.4.2** | `pytest_addoption` / `pluginmanager` 插件名 | conftest hook 契约变化 | `pyproject.toml` |
| **Python** | **3.12.6** | 全项目 | — | `pyproject.toml` |
| **browser-harness** | **0.1.13** | 录屏采集入口 `drain_events()` | 采不到帧 | `pyproject.toml` 已钉 |

> 表里标「**需校验**」的五项是本机环境，**是二开要补的部分**；其余已被依赖文件钉住。

`versions.json` 就是这张表的机器可读版，供 `install_report_plugin.py` 校验。

### 8.5 eval（守住"静默失败"）

在 `scripts/check_report_plugin.py` 或既有 skill 的 evals 里加：

1. 报告里**确实**有 `plugins/step-video/index.js`，且 `index.html` 真的引用了它；
2. 实际版本与 `versions.json` **一致**；
3. 重复执行后处理脚本**不重复插入**；
4. 把 `versions.json` 里某一项**故意改错**，脚本必须**报错退出**（负向断言）。

理由与项目里其他 eval 一致：**"报告生成了但没插件"这种失败是静默的**，
只有断言能兜住。

---

## 9. 改造后目录结构

```text
jev-ultrafast/
├── jev_ultrafast/
│   ├── agent.py                    [改] 接收执行返回值；history 补元素标识；透传 wait_stable
│   ├── browser.py                  [改] 记录 last_wait；wait_stable 开关
│   ├── snapshot.js                 [不动]
│   ├── model.py                    [不动]
│   └── framework/
│       ├── config.py               [改] 新增三个内置默认
│       ├── e9_config.py            [改] 读 config.json 新键
│       ├── runner.py               [改] 步骤拆分 / 参数 / 附件 / 等待 / 录屏接线
│       ├── reporting.py            [改] attach_decision 补字段；新增 attach_execution
│       ├── report_params.py        [新] 步骤参数的两个把手（私有 API 收口在此）
│       ├── video.py                [新] 录屏（CDP screencast + 采集线程 + epoch）
│       ├── encode.py               [新] jpeg 帧 → mp4（concat + cfr 30）
│       └── report_plugin/
│           └── step-video/         [新] Allure 插件（index.js / styles.css / versions.json / README.md）
├── scripts/
│   └── install_report_plugin.py    [新] 后处理：版本校验 + 装插件 + 改 index.html
├── tests/
│   ├── conftest.py                 [改] 三个 pytest 选项
│   ├── test_report_params.py       [新]
│   ├── test_video_modes.py         [新]
│   └── test_report_plugin.py       [新]
├── config.example.json             [改] 新增三个键（占位模板）
└── AGENTS.md / README.md           [改] 同步契约与命令
```

---

## 10. 分步实施计划

> 每个里程碑都给「改什么 / 怎么验 / 期望看到什么」。
> **判据一律是"落盘能看到"，不是"代码写了"。**

### M1 · 步骤边界拆分（**先做，其余都依赖它**）

| | |
|---|---|
| 改动 | `framework/runner.py`（`_tick_once` 拆掉、循环重写） |
| 命令 | `uv run --env-file .env pytest tests/test_nl_cases.py --nl --case demo-weaver-site-tour --reruns 0 --alluredir=report/allure-results` |
| 期望 | ① 执行步骤时长**不再是 0**；② 决策步骤时长明显下降；③ 步骤标题与编号成对；④ `DONE` 那轮少一个执行步骤 |
| 判据 | 附录 A 第 6 条的命令逐步骤打印时长，人工核对一遍 |
| 回归 | `uv run ruff check .`、`uv run pytest`（离线 36 passed / 3 skipped） |

### M2 · 参数表与附件

| | |
|---|---|
| 改动 | 新增 `framework/report_params.py`；改 `reporting.py`；改 `agent.py`（history 补字段） |
| 单测 | `tests/test_report_params.py`（含**反向断言**：进入后改 `.params` 无效） |
| 命令 | 同 M1 |
| 期望 | 决策步骤：参数表 + `Jev 决策概率` + 请求体附件；执行步骤：参数表 + `执行请求与返回` 附件 |
| 判据 | §4.6 的落盘校验脚本输出 |

### M3 · 配置开关

| | |
|---|---|
| 改动 | `config.py` / `e9_config.py` / `conftest.py` / `runner.py`（`resolve_report_options`） |
| 单测 | `tests/test_video_modes.py` 里先测优先级解析（纯函数） |
| 命令 | `pytest --video-record=0 ...` 与不带参数各跑一次，比 `resolve_report_options` 的返回值 |
| 期望 | 不传 → 取 config.json；传了 → 取传值；**两者都能在报告/日志里看到实际生效值** |
| 判据 | 把生效值写进「执行摘要」附件（`录屏档位` / `等待开关`），报告里能核对 |

### M4 · 等待步骤

| | |
|---|---|
| 改动 | `browser.py`（`last_wait` + `wait_stable`）、`agent.py`（透传）、`runner.py`（`_wait_step_if_significant`） |
| 期望 | E9 用例里出现「第 N 步 · 等待页面稳定」，**时长不是 0**，参数表有 `等待耗时ms` / `是否等到稳定` |
| 判据 | 同上落盘校验；另确认 **<200 ms 的短等待没有建步骤** |
| 待实测 | §13 第 2、3 项 |

### M5 · 录屏与合成

| | |
|---|---|
| 改动 | 新增 `framework/video.py`、`framework/encode.py`；`runner.py` 接线 |
| 单测 | `test_video_modes.py`：三档 × 通过/失败的清理决策（纯函数，不碰浏览器） |
| 命令 | `--video-record=1` 跑演示用例 |
| 期望 | ① 报告里出现 `<video controls>`；② 时长与用例执行时长吻合；③ `artifacts/` 下**没有残留帧目录** |
| 判据 | 逐帧对齐误差抽测：随机取 3 个步骤，按 `(step.start − epoch)/1000` seek，核对画面 |
| 待实测 | §13 第 1 项（端到端 A/B） |

### M6 · 二开插件与版本校验

| | |
|---|---|
| 改动 | 新增 `framework/report_plugin/step-video/**`、`scripts/install_report_plugin.py`；`test_report_plugin.py` |
| 命令 | 见附录 A 第 3b 条 |
| 期望 | ① 用例页出现时间轴区块；② 点步骤视频跳到对应位置；③ 播放时高亮当前步骤；④ 故意改错版本 → 脚本报错退出 |
| 判据 | 与探针插件相同的验证方式（探针已跑通：29/29 步骤拿到时间区间、零 JS 报错） |

### M7 · 收尾：文档与契约

| | |
|---|---|
| 改动 | `AGENTS.md`（新契约）、`README.md`（命令与配置项）、`config.example.json` |
| 期望 | 新增的契约能一句话说清：录屏三档、等待开关与它的真实代价、二开版本锁定 |
| 判据 | 按 README 从零跑一遍（含 `allure serve` 看视频能否拖动） |

---

## 11. 实施进度台账

> **本文随实施更新。** 只记录**真跑过的结果**，没跑的一律写「未开始」。
> 每个里程碑完成后：填「完成日期 / 实测数据 / 遗留问题」三列，并在 §0.3 更新概览。

| 里程碑 | 状态 | 完成日期 | 实测数据 | 遗留问题 |
|---|---|---|---|---|
| **M0** 实施方案定稿 | ✅ 已完成 | 2026-09-24 | 本文件 | — |
| **M1** 步骤边界拆分 | ⬜ 未开始 | | | |
| **M2** 参数表与附件 | ⬜ 未开始 | | | |
| **M3** 配置开关 | ⬜ 未开始 | | | |
| **M4** 等待步骤 | ⬜ 未开始 | | | |
| **M5** 录屏与合成 | ⬜ 未开始 | | | |
| **M6** 二开插件与版本校验 | ⬜ 未开始 | | | |
| **M7** 文档与契约收尾 | ⬜ 未开始 | | | |

### 实施前的基线（2026-09-24 实测，用于前后对比）

| 指标 | 基线值 | 来源 |
|---|---|---|
| 一条 12 步用例的执行步骤总时长 | **8 ms** | 分析报告 §1.4 |
| 同一条用例的决策步骤总时长 | **23,006 ms**（占 78%） | 同上 |
| 决策请求耗时（TypeSafe） | 中位 **393 ms**（375–554） | 同上 |
| 文本模型调用耗时 | **3328 ms**（占该步 79%） | 同上 |
| 报告步骤数（一条 12 步用例） | **29** | 探针插件实测 |
| 离线回归 | `ruff` 全绿；`pytest` **36 passed / 3 skipped** | 一期收尾 |
| skill evals | 3/3 全绿（28/28 断言） | 一期收尾 |

> **M1 完成后必须回填这张表的对照值**——否则"拆分有没有用"就只能靠感觉说。

---

## 12. 风险与回滚

| 风险 | 等级 | 对策 | 回滚 |
|---|---|---|---|
| **步骤拆分改坏既有报告语义** | **高** | M1 单独立项、前后对比逐步骤核对；四个语义逐条列了保法（§3.3） | `runner.py` 的改动集中在一个文件，`git revert` 单个提交即可 |
| **`-1` 档误删证据** | **高** | "通过"的边界画严（§6.4）；删除动作写进报告；单测覆盖"通过但不健康" | 把 `--video-record=1` 一档切回去，代价只是磁盘 |
| **库公开 API 被框架需求推动扩张** | 中 | 两处新参数默认值保持行为不变；评审时明确边界 | 去掉关键字参数 |
| **插件静默失效** | 中 | `versions.json` + 安装时校验 + eval | 关掉后处理步骤，报告回到一期形态 |
| **`#t=` 式"省事方案"被误用** | 中 | 已在分析报告 §4.3.4 记为"不可行"，插件一律用 JS 设 `currentTime` / Blob URL | — |
| **报告体积膨胀** | 低 | 请求体附件只挂 `questions`；视频三档可控；`report/` 已 gitignore | 调低档位 |
| **帧目录撑爆磁盘** | 中 | `finally` 清理 + 断言"无残留" | — |

**回滚粒度**：M1 / M2+M3 / M4 / M5 / M6 各自独立提交，
**每个里程碑一个提交**，出问题能单独回退而不牵连其他。

---

## 13. 必须在实施期补的实测（本方案不能替它们下结论）

| # | 测什么 | 怎么测 | 为什么必须测 |
|---|---|---|---|
| 1 | **录屏的端到端 A/B** | 演示用例「开录屏 / 不开录屏」各跑一次，比总耗时、决策请求数、浏览器动作数 | 分析报告 §4.1 只测了**观察阶段**的代理指标（+3–6 ms/次），**不足以支撑"端到端无影响"** |
| 2 | **等待开关的 A/B** | 同一用例「开等待 / 关等待」各跑一次，**比决策请求数**（不是比耗时） | §7.4 的判断是"关掉可能更贵"，**没测过**；而这正是决定批量执行要不要关的依据 |
| 3 | **等待步骤在 E9 上的收益** | 跑一条 E9 用例，看「等待页面稳定」步骤的实际耗时分布 | 200 ms 的门槛是按 demo 站点的数据定的，E9 上是否合适要实测 |

> 三项都**不花钱也能做前两项的一半**（前两项需要跑用例，会产生一次付费调用）。
> 建议 M4 完成后一起做，一次跑批拿到三组数据。

---

## 附录 A：命令清单

```bash
# 0. 前置
bash .claude/skills/run-jev/scripts/browser.sh          # 起带 CDP 的 Chrome
test -f .env && test -f config.json && echo "配置就绪"

# 1. 离线回归（每改一次都跑）
uv run ruff check .
uv run pytest                                           # 期望 36 passed / 3 skipped（并随本期新增单测增长）

# 2. 一条演示用例（不需要 E9、不需要 config.json）
uv run --env-file .env pytest tests/test_nl_cases.py \
  --nl --case demo-weaver-site-tour --reruns 0 \
  --alluredir=report/allure-results

# 3. 出报告（必须先清，否则跨次累积）
rm -rf report/allure-report
allure generate report/allure-results -o report/allure-report --clean

# 3b. 装二开插件（必须在 generate 之后）
uv run python scripts/install_report_plugin.py report/allure-report

# 3c. 看报告 —— 用 allure serve 而不是 python -m http.server
#     后者不支持 HTTP Range，视频拖不动；插件也可以把视频读成 Blob URL 绕开
allure serve report/allure-results

# 4. 三档开关各验一次
uv run --env-file .env pytest tests/test_nl_cases.py --nl --case demo-weaver-site-tour --video-record=0 ...
uv run --env-file .env pytest tests/test_nl_cases.py --nl --case demo-weaver-site-tour --video-record=1 ...
uv run --env-file .env pytest tests/test_nl_cases.py --nl --case demo-weaver-site-tour --video-record=-1 ...

# 5. 等待开关（先做 A/B，再决定要不要写进批量脚本）
uv run --env-file .env pytest tests/test_nl_cases.py --nl --case demo-weaver-site-tour --no-wait-stable ...

# 6. 步骤时长归因（M1 的前后对比就靠它）
uv run python -c "
import json, glob, sys; sys.stdout.reconfigure(encoding='utf-8')
d = json.load(open(sorted(glob.glob('report/allure-results/*-result.json'))[0], encoding='utf-8'))
for s in d['steps']:
    print(f\"{s['stop']-s['start']:7d} ms  {s['name'][:56]}  attach={len(s.get('attachments',[]))}\")
"

# 7. 落盘校验：参数与附件真的进报告了吗
#    见 §4.6 的脚本

# 8. 二开插件与版本校验的 eval
uv run python scripts/check_report_plugin.py            # M6 新增
```

## 附录 B：提交前自检（不可提交内容）

```bash
git check-ignore -v config.json .mcp.json .env          # 三者都必须命中
git status --porcelain -uall | grep -v '^?? artifacts/' # 只看受版本管理的改动
git status --porcelain -uall | grep -E "config\.json|\.mcp\.json|\.env" && echo "❌ 敏感文件将入库" || echo "✅ 干净"
```

```
❌ config.json（E9 地址与账号）、.mcp.json（内网地址）、.env（模型密钥）
❌ 任何 cookie 值、TYPESAFE_API_KEY / TEXT_MODEL_API_KEY
❌ report/ 与 artifacts/（报告产物、录屏、原始帧、请求体附件）——两个都已 gitignore，保持这样
❌ 临时文件落在仓库根目录（AGENTS.md 已立规：一律写 artifacts/）

✅ 可以提交：
   · 框架代码与新增模块（framework/report_params.py、video.py、encode.py）
   · 二开插件源码与后处理脚本（report_plugin/**、scripts/install_report_plugin.py）
   · versions.json / config.example.json / README / AGENTS.md
   · cases/*.yaml（用 {{ base_url }} 变量，不写真实地址）
```

> 二开新增的全是**展示层**代码，不含凭据；它读的是报告 `data/` 里的数据，不额外请求任何服务。